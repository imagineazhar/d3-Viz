# Connected Signals

Force-directed network with draggable nodes and weighted links.

- Encoding: link stroke width by edge weight
- Interaction: drag nodes to inspect local structure
- Data: local synthetic node-link graph in `data.ts`